
    - name: Set authorized keys from Ansible management host
      authorized_key:
        user: root
        state: present
        key: '{{ item }}'
      with_file:
        - ./.cache/id_rsa.pub

